# LY 项目 AI 协作协议

本文件适用于整个仓库。任何新 AI、自动化代理或开发者开始工作时都必须遵守。

## 新对话启动顺序

1. 阅读 `docs/current-status.md`，以它作为“当前状态”的唯一权威入口。
2. 执行 `git status --short --branch` 和 `git log --oneline -5`，核对实际仓库状态。
3. 阅读 `docs/project-architecture.md` 和 `docs/decisions.md`。
4. 阅读 `docs/work-log.md` 的最近记录；仅在任务涉及历史需求时再查阅 `docs/conversation-handoff.md`。
5. 按 `docs/current-status.md` 的“下一步明确动作”继续；文档与代码或 Git 冲突时，以代码和 Git 为事实来源，并先修正文档。

## 检查点规则

发生以下任一事件后，立即更新 `docs/current-status.md`，不要等待整轮对话结束：

- 需求或验收标准确认；
- 采用、拒绝或延后重要想法；
- 完成一组代码或文档修改；
- 测试成功或失败；
- 发现阻塞、风险或架构偏差；
- 准备开始大范围修改；
- 任务暂停、对话可能结束或存在中断风险。

每个检查点必须写明：完成内容、修改文件、代码可运行状态、测试结果、下一步唯一推荐动作、待确认问题、未提交修改和推送许可。每次检查点同时向 `docs/work-log.md` 追加简短记录，不覆盖历史。

## 想法与决策

- 未确认想法先写入 `docs/ideas.md`，使用唯一 `IDEA-NNN` 编号。
- 想法状态只能是：`待评估`、`已接受`、`已拒绝`、`延后`。
- 接受的长期决定写入 `docs/decisions.md`，使用唯一 `DEC-NNN` 编号。
- 决策状态只能是：`生效`、`被替代`、`撤销`。
- 不得把待评估想法写成已经确定的需求；拒绝或延后的想法保留简短理由和重新评估条件。

## Git 与发布边界

- 当前稳定远端基线为 `v1.0.40`；本地研发从 `local/v1.0.42` 开始。
- 允许在功能完整、文档同步且相关测试通过后创建本地里程碑提交。
- 未完成但必须防止中断丢失时，可使用明确的 `checkpoint:` 本地提交。
- 未经项目所有者明确同意，禁止执行 `git push`、创建远端分支或标签、发布版本或把本地研发合并到远端发布分支。
- “继续开发”“测试通过”“完成修改”均不代表推送许可。
- 获得推送许可前，必须先汇报待推送提交、文件差异、测试结果和版本号。

## 验证要求

- 修改后先运行最相关检查，再运行 `npm test`。
- 交接文档修改至少运行 `npm run handoff:check`。
- 版本、分支或任务变化时，必须同步 `docs/current-status.md`。
- 不修复与当前任务无关的问题；发现后记录到状态、想法或工作日志。
